# ANCHOR-516-04: Clean full rebuild of 516 on fixed tip; freeze product;
# re-cut --full anchor + P1 golden from 516; retirement list; docs

Date issued: 2026-08-18
Push: only on Milan's authorization (single authorization expected at
close for the whole 96aa0d6..anchor-recut series).

Premise check: 516-03 landed three fixes (empirical ERR cache +
INV-ERR-MODE-01, comp_qa cleanup, sat-limit one-authority) at tip
4a65675, and rebuilt 516 -> be6191e0 (MAG identical to de6f7c8, ERR
empirical). But that rebuild consumed a pre-existing
masterstars_full_match.csv annotated under 0.85 - the product is not
wholly generated by the fixed tip. Separately, --full FAIL against the
435 golden is expected and terminal: Milan confirms ROT out-of-scope
is deliberate policy; the 435 golden predates it and is retired by
design, not repaired. This task produces the final clean product and
cuts all frozen references from 516.

Pre-registered expectations (state PASS/DEVIATE for each):
E1. Re-annotation: saturated count 23 -> 24; star 1497853802778923392
    leaves comparison_stars.csv.
E2. ct_n_comp decreases vs 2346; ct_c1 MAY move (legitimate fit-input
    change). Record old/new ct_c1.
E3. MAG expected byte-identical to de6f7c8/be6191e0. If ANY MAG bit
    moves: STOP, report per-target delta, architect review before
    anything else.
E4. ERR remains empirical; per-term: sem/scint/sys deltas = 0,
    photon-only differences vs de6f7c8.
E5. saturate_limit columns in all fresh products carry the 0.80
    authority (52428.0); no 55704.75 anywhere in fresh output.

## Part A - Clean rebuild from Phase 0
1. On tip 4a65675 (or current if docs commits advanced it; report
   actual): full Phase 0 -> MASTERSTAR zone re-annotation -> Phase 1
   -> Phase 2A on 516, PFS ON, export_err_mode=calibrated, canonical
   headless harness. No pre-existing catalog/pool/QA files consumed:
   verify by provenance (mtimes/stage stamps all post-run-start) that
   every product file was written by this run.
2. Record final product SHA (core + extended). This SHA supersedes
   both de6f7c8 and be6191e0 in the ledger
   (superseded-with-pointer chain: de6f7c8 -> be6191e0 -> final;
   reasons: ERR-mode fix; catalog authority + clean provenance).
3. Evaluate E1-E5. Runtime per phase (Rule 0.3).

## Part B - Exports + acceptance numbers on the final product
4. Regenerate BO CVn AAVSO + VarAstro exports; SUBMIT-01 checklist
   full re-run; diff vs the 515-era on-disk files (never submitted;
   new files supersede). Report MAGERR row-change count at export
   precision and confirm MAG 134/134 identity (E3 gate applies).
5. Record: BO median err, 01B check meters (BO + FW), per-term
   decomposition, gating census (expect 48 LCs; CV CVn
   per_frame_saturation).

## Part C - Re-cut all frozen references from 516
6. Re-cut --full anchor: reference draft = 516 final product,
   canonical headless mode, PFS ON, current config policy (ROT
   out-of-scope included by design). Snapshot inputs frozen so the
   gate compares against frozen state, not live-drift (the 435
   lesson: a golden photometering a live draft under evolving config
   is not a reproducibility gate).
7. Cut new P1 golden from 516 (mini allowed; MUST include
   cal_diag.json). Register SHAs in the validation ledger; retire
   VL-ANCHOR-WCSINV / VL-P1-GOLD 435 entries as
   superseded-with-pointer.
8. Repoint every remaining 435/435_p1mini reference
   (session_baseline_check.py, test_invariants_p1_golden.py,
   build_p1_golden_mini.py, any hardcoded expected SHAs incl. the
   stale 5bccd85a). No dead references left.
9. Fire proof: session_baseline_check.py --fast AND --full OVERALL
   PASS on the final tip. Stale-golden tests pass, not xfail.
10. Runtime per part (Rule 0.3).

## Part D - Retirement list + docs
11. Final reference grep; output DELETE-OK list for Milan: expected
    435, 435_p1mini, 435_snapshot_skysurface, 436, 437, 509, 513,
    514, 515 (no submission hold - nothing was uploaded; Part B diff
    is on record). Milan deletes manually.
12. Docs (separable commit): STATE (new anchor, SHA lineage, canonical
    run mode), ROADMAP (close FULL-ANCHOR-RECUT, P1-RECUT,
    A-1-435-RECUT; note 0.85-gate fix), DECISIONS (canonical anchor
    mode; INV-ERR-MODE-01; one-authority completion incl. MASTERSTAR
    writer; anchor-from-clean-rebuild principle), JOURNAL, ledger,
    PARAMS (saturate_limit_fraction default 0.80), CHANGELOG.

## Constraints
- Separable commits: rebuild/freeze artifacts, anchor re-cut,
  golden re-cut, docs.
- Leftover 2026-08-17 tracked diffs stay excluded from all commits.
- No deletes by Cursor. Raw numbers under
  dev/results/context/session_20260818_anchor_516_04/ (Rule 0.2).
- Standing authority: reject on premise contradiction.

## Docs impact
As Part D step 12.
