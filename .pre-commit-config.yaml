# BLE001 / E722 regression guard — new broad excepts must be explicitly noqa'd or fixed.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.6
    hooks:
      - id: ruff
        name: ruff broad-except guard (BLE001, E722)
        args: [--select, BLE001, E722]
